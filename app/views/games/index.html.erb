<h1>Übersicht aller Games bei IchSpiele</h1>

<p>
  <%= link_to "Neues Spiel hinzufügen", new_admin_game_path %>
</p>

<ul>
  <% @games.each do |game| %>
    <li><%= link_to game.name, game_path(game) %> | <%= link_to "Bearbeiten", edit_admin_game_path(game) %></li>
  <% end %>
</ul>